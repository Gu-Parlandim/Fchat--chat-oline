import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  align-items: center;
`;

const Box = styled.div`
  background-color: #87ffe9;
  border: 2px solid #000;
`;
const BoxText = styled.p`
  font-size: 1.9rem;
  padding: 3px;
`;

const Text = styled.p`
  font-size: 1.2rem;
`;

export { Wrapper, Box, BoxText, Text };
